<p id="notice"><%= notice %></p>

<h1>Listing Parking Lots</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>City</th>
      <th>Postal code</th>
      <th>Province</th>
      <th>Country</th>
      <th>Phone number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parking_lots.each do |parking_lot| %>
      <tr>
        <td><%= parking_lot.name %></td>
        <td><%= parking_lot.address %></td>
        <td><%= parking_lot.city %></td>
        <td><%= parking_lot.postal_code %></td>
        <td><%= parking_lot.province %></td>
        <td><%= parking_lot.country %></td>
        <td><%= parking_lot.phone_number %></td>
        <td><%= link_to 'Show', parking_lot %></td>
        <td><%= link_to 'Edit', edit_parking_lot_path(parking_lot) %></td>
        <td><%= link_to 'Destroy', parking_lot, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Parking lot', new_parking_lot_path %>
